/**
 * User-facing messages for the Lensy analysis pipeline.
 * Centralised here so frontend and backend stay in sync.
 */

export const SPA_USER_MESSAGE =
    'This page loads its content with JavaScript, which we can\'t fully execute yet — so we\'re seeing an empty shell rather than your docs.\n\n' +
    '**Workarounds you can try right now:**\n' +
    '- Submit a deep-link sub-page that\'s pre-rendered or static\n' +
    '- Try a cached version of the page (e.g. a Google cache link)\n\n' +
    '**Coming soon:** automatic JavaScript rendering — we\'re rolling it out shortly.';
